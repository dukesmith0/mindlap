# Bugs
Next ID: 20

## Open
#19 [LOW] Avatar initial letter renders slightly above the geometric center of the disc. Likely Courier Prime cap-baseline metrics: monospace caps don't sit at the line-box center even with `align-items: center`. Fix candidates: small `padding-top` or `transform: translateY()`, or switch to SVG `text` with `dominant-baseline="central"`. File: `components/ui/Avatar.tsx:9-30`.
#18 [LOW] No XP bar surfaced anywhere in the app. Topbar shows "Lv X" only; `profiles.xp bigint` exists but is unread. Phase 6 owns the full XP system (events, multipliers, streak bonus, double-XP). Decide: ship a minimal passive XP bar in Phase 2.5 vs wait for Phase 6 deliverable. Defaulting to defer until Phase 6 unless prioritised.
#17 [MEDIUM] "<- today" back link on `/play/[game]` looks unclickable. Rendered as muted-color Link with no hover state, no underline, no cursor change. Daily users misread it as a static label. Fix: give the back link an explicit affordance (accent color on hover at minimum; or restyle as a small `.btn-link` chip; or pair with a sidebar in Phase 4 that owns navigation). Same class of issue as #15. File: `app/(authed)/play/[game]/page.tsx:35-37`.
#16 [LOW] /favicon.ico 404s on every page load. Console-error noise; no functional impact. Fix: add a 1px transparent or text-glyph favicon under `app/icon.png` or `app/favicon.ico` (Next App Router auto-serves).
#15 [MEDIUM] /settings has no obvious "back" affordance. User lands there from the avatar in the topbar but has no in-page exit. Fix: add a back-to-today link or a topbar nav element. Discuss whether to ship the sidebar from zetamac-pure.html now (Phase 4 territory) or just add a single back link for v1.
#14 [MEDIUM] Digit Span sequence visually overflows the column at length 10+. Current style (`game-text-digit`: 80px / letter-spacing 6px) puts ~10 digits at the edge of the 720px - 128px main column. Spec needs digits to render up to length 15 (the MAX_LENGTH cap) without clipping. Discuss optimal behavior: (a) `font-size: clamp(36px, 8vw, 80px)` so the text shrinks fluidly, (b) a step function based on `length` (`80px` <=8, `60px` 9-11, `48px` 12-15), (c) wrap to two lines once we exceed N digits. Open question: does shrinking past ~48px hurt memorisation accuracy by reducing visual chunking? Also consider: chunked spacing (e.g. groups of 3-4 digits) instead of even letter-spacing.

## Deferred

## Resolved
#13 [HIGH] Dev server crashed onboarding with a CSP-blocked dynamic-script error, then "Failed to fetch" on the Server Action. Production CSP `script-src` lacked `'unsafe-eval'`, which the React dev bundle requires for source-map reconstruction. Cascaded into the server-action client breaking. Fix: gate `'unsafe-eval'`, `ws://localhost:*`, and absence-of-`upgrade-insecure-requests` behind `NODE_ENV === "development"` in `next.config.ts`. Production keeps the strict policy. (manual report, resolved 2026-04-28)
#12 [HIGH] N-Back component leaked timeouts on unmount. setTimeout ids were tracked via `void` rather than a Set, so unmounting mid-stream let `setLetter("")` fire on an unmounted component. Fix: collect every timer in a `Set<>`, clear all in cleanup. (vibe:reviewer, resolved 2026-04-28)
#11 [HIGH] `<button>` nested inside `<Link>` produced invalid HTML across `today/page.tsx`, `play/[game]/page.tsx`, and `ResultScreen.tsx`. Anchor wrapping a button breaks HTML5 validation and some screen readers. Fix: introduced `.btn-link` class for server-component links; ResultScreen "next game" button uses `router.push`. (vibe:reviewer, resolved 2026-04-28)
#10 [LOW] Signup form had only one password field. UX risk: typo'd password locks user out, requires reset flow. Fix: added `confirm_password` field client+server validated. (manual review, resolved 2026-04-28)
#9 [LOW] Onboarding "let's play" button rendered the literal text `let's play -&gt;`. JSX entities decode in TEXT nodes, not in JS string expressions. Line 132 of `OnboardingFlow.tsx` had the entity inside a string literal. Fix: replaced with literal `->`. (manual report, resolved 2026-04-28)
#8 [CRITICAL] Onboarding submit returned `permission denied for table profiles`. Migrations 0001-0005 ran via the Management API SQL endpoint, which creates tables owned by `postgres` and bypasses Supabase's normal default-privileges seeding for anon/authenticated/service_role. Result: every public table had only inherent REFERENCES/TRIGGER/TRUNCATE — no DML grants. Every authenticated request to RLS-enabled tables hit "permission denied" before RLS could even run. Fix: 0006_restore_role_grants.sql GRANTs SELECT/INSERT/UPDATE/DELETE on all public tables + ALTER DEFAULT PRIVILEGES so future tables inherit the grants. (manual report, resolved 2026-04-28)
#7 [HIGH] Email-confirmation link 404'd. The OAuth/email-confirm route lived at `app/(auth)/callback/route.ts`, but `(auth)` is a Next.js route group (parentheses) that does NOT contribute to the URL, so the file served `/callback`, not `/auth/callback`. The proxy allowlist, `actions/auth.ts` `emailRedirectTo`, and Supabase email templates all targeted `/auth/callback`. Fix: moved the route to `app/auth/callback/route.ts` (real segment). (manual signup test, resolved 2026-04-28)
#6 [HIGH] Signup hit "Database error saving new user". `generate_friend_code()` (0002) was missed when 0004 moved `friend_code` to `profile_secrets` (collision check scanned the dropped column), and its `search_path = public` excluded `extensions` where pgcrypto's `gen_random_bytes` lives. Both manifested inside the auth.users trigger. Fix: 0005 rebinds the collision check to `profile_secrets` and sets `search_path = public, extensions`. (manual signup test, resolved 2026-04-28)
#5 [HIGH] `friend_code` on `profiles` was publicly enumerable via PostgREST. Fix: 0004 moves friend_code to `profile_secrets` (owner-only RLS) + `find_user_by_friend_code(text)` RPC. (Phase 1 security audit, resolved 2026-04-28)
#4 [HIGH] Open-redirect via `next` form param in signin/Google/callback. Fix: `safeNext()` validates same-origin (single leading slash, no protocol-relative). (Phase 1 reviewer, resolved 2026-04-28)
#3 [HIGH] `lib/supabase/proxy.ts` did not handle signed-in user with no profile (handle_new_user race/failure). Fell through to authed pages profile-less. Fix: missing profile = redirect to `/onboarding`. (Phase 1 reviewer, resolved 2026-04-28)
#2 [HIGH] Proxy `/profile/` allowlist also matched `/profile/me/*`, leaving private namespaces anonymous-readable. Fix: `isPublicPath()` excludes `/profile/me*` before allowing `/profile/<username>`. (Phase 0 reviewer, resolved 2026-04-28)
#1 [HIGH] Proxy redirect dropped Supabase session cookies refreshed during `getUser()`. Fix: copy `response.cookies.getAll()` onto the redirect before returning. (Phase 0 reviewer, resolved 2026-04-28)
