# Bugs
Next ID: 44

## Open
#43 [MED] Badges render as identical accent dots; should use a per-key emoji to give each badge personality (e.g. streak_3/7/30/100 -> 🔥, pb_first_<game> -> 🏆 or game-themed icon, all_seven_today -> 🎯, achievement badges (Phase 5.5) -> 🧠/⚡/etc.). Map lives next to the badge `criteria` jsonb (or in a new `lib/badges/icons.ts` keyed by badge_key). Update both the profile badge-wall list (`badge-dot` span) and any future footer surface. File: `components/ui/Badge.tsx` (new) + `app/profile/[username]/page.tsx`. Pairs with decision-update in decisions.md to extend the "no emoji" exception list to badges.
#42 [MED] /profile/[username] needs a GitHub-style heatmap of games played per day (90-day window per `decisions.md`). Cells colored by intensity (no plays = `--line`, low = light accent, high = full accent). Already on the Phase 5.5 list in plans.md but flagging as near-term since the user requested it concretely. Implementation: SELECT distinct `played_pt_date` counts from `submissions` for the last 90 days for the profile owner, render as a 13-week × 7-day grid of 11×11 squares with 1px borders. Pure inline SVG or div grid, no extra dep. File: new section on `app/profile/[username]/page.tsx`.
#41 [LOW] Remove the per-card mini leaderboard preview rows from /today. Each game card currently renders top-3 today scores under the meta line, which clutters the card and competes with `play ->`. Full-fidelity rankings already live on /leaderboards (linked from /today footer). Fix: drop the `previews` Map fetch in `app/(authed)/today/page.tsx` (7 round-trips removed -> Phase 11 perf bonus) and the `<div className="lb-preview">` block in `TodayCard.tsx`. Keep the `browse all leaderboards ->` link.
#40 [MED] /leaderboards rows show plain text usernames with no avatar and no link. Each row should render the avatar disc (Avatar component, color-from-profile, 22px) inline before the username, and the username should be a `<Link href="/profile/<username>">`. Requires extending the leaderboard SELECT to embed `profiles.avatar_color, display_name`. File: `app/leaderboards/page.tsx` plus the SELECT projection (currently only pulls `username` via `profiles(username)`).
#39 [LOW] Hovering the blue `*` (core-game indicator) should surface a themed tooltip explaining "cognitively relevant game" (or similar). Currently no `title` and no custom tooltip, so meaning is opaque to first-time users. Fix: build a tiny zetamac-pure-themed tooltip component (1px accent border, no shadow, square corners, accent text) and wire it to every `.game-card-star`. Files: new `components/ui/Tooltip.tsx`; touch-points include `app/(authed)/today/TodayCard.tsx`, `app/profile/[username]/page.tsx`, GameShell ready screen.
#38 [LOW] Profile per-game card is missing total-plays (lifetime `plays_submitted` for that game). Currently shows 30d plays only. Fix: add a 5th query summing `daily_aggregates.plays_submitted` over all dates for the user+game, render as a "TOTAL PLAYS" column. Folds into the #26 "collapse to one RPC" follow-up. File: `app/profile/[username]/page.tsx`.
#37 [MED] /profile/[username] per-game grid should align like a proper table: every game row shares the same columns at the same x-offsets (game name + PB + set + worst + 7d median + 30d plays + total plays). Current flex-wrap layout puts each game's stats on their own track so columns don't align across games visually. Fix: switch to a CSS grid with explicit column tracks (or a real `<table>`), one row per game. File: `app/globals.css` `.profile-game-grid` + `app/profile/[username]/page.tsx`.
#36 [LOW] StreakRibbon pulses the entire row (🔥 + number + "days"). Only the 🔥 should fade in/out. Fix: move `animation: streak-pulse 2s` from `.streak-ribbon` (parent span) onto just the inner emoji span; number/units stay steady. File: `components/ui/StreakRibbon.tsx` + `app/globals.css`.
#35 [LOW] `Countdown` shows 3/2/1 for 800ms each but "go" only 400ms. User wants equal on-screen time. File: `components/games/Countdown.tsx`. Fix: bump the final timeout so 3/2/1/go each get the same duration (e.g. 700ms × 4 = 2.8s total, or pick whatever feels right).
#34 [LOW] /today search input should be right-aligned with the right edge of the `play ->` button column and vertically level with the "Today's Games" h1 (currently sits below the date subtitle on its own row). Fix: restructure `TodayHeader` so title-block and search are flex-row on desktop with `align-items: flex-start` and the search anchors to the right gutter at the same baseline as the h1.
#33 [LOW] /settings DELETE ACCOUNT section heading + "delete my account" button should render in red to convey destructive action. File: `app/(authed)/settings/SettingsClient.tsx` `DangerZone` + matching CSS in `app/globals.css`. Fix: add `.danger-h2` (red) on the h2 and a `.btn-danger` modifier on the button (red border + red text + hover red fill).
#30 [MED] Pre-submit comparison view on ResultScreen. Show user's current PB, worst, 7d median, leaderboard rank for this game, plus a green `(+N)` or red `(-N)` delta indicating what would change if they submit this play. Implement as a Server Action `getPreSubmitContext(game_key, score)` returning current+projected values; render in ResultScreen above the submit button.
#26 [LOW] Profile page does 28 supabase round-trips per render (4 queries × 7 games). Acceptable for v1 traffic; collapse to one `get_public_profile_stats(user_id)` RPC in Phase 11. File: `app/profile/[username]/page.tsx:82-146`.
#19 [LOW] Avatar initial floats above center (Courier Prime cap-baseline). `components/ui/Avatar.tsx:9-30`. Fix: padding-top 1px or SVG `dominant-baseline=central`.
#16 [LOW] `/favicon.ico` 404 console noise. Fix: ship `app/icon.png` or `app/favicon.ico`.
#15 [MED] `/settings` has no back affordance. Fix: back-link or topbar/sidebar nav.
#14 [MED] Digit Span overflows column at length 10+ (`game-text-digit` 80px / spacing 6px in 720-128 col). Discuss before fix: clamp() vs step-fn vs chunked spacing. Must fit lengths 11-15.

## Deferred

## Resolved (2026-04-28)
#32 [HIGH] `process_submission` raised `column reference "best" is ambiguous` on every submit. Cause: `RETURNS TABLE(best, worst, mean, median, ...)` declares OUT params that shadow daily_aggregates columns inside the function body. Fix: 0010 changes the return type to `jsonb`; `submitScoreAction` reads the JSON object directly.
#31 [HIGH] /profile/<username> per-game stats grid garbled (labels colliding across columns). Fix: collapsed `.profile-game-grid` to single column; stats render as wrap-flex with one label/value per stat.
#29 [LOW] Verified: pins ARE retained across sessions (RLS-gated SELECT on every render). Closed by inspection.
#28 [MED] /today header had no search. Fix: `TodayHeader` + `TodayList` client island with name/key client-side filter.
#27 [LOW] Participation cap coupled to bonus mult. Fix: 0009/0010 set `v_part_cap = 5 * v_bonus_mult` (10/play, 10/day on bonus days).
#25 [MED] Sidebar Profile link never lit up on canonical `/profile/<username>`. Fix: Sidebar special-cases Profile to active on any `/profile/...`.
#24 [HIGH] `award_xp` ignored `p_multiplier`; `xp_awarded` desynced from `profiles.xp` on bonus games. Fix: 0009/0010 pre-multiply at the caller, pass `p_multiplier=1.0`. xp_events.amount and profiles.xp now agree.
#23 [LOW] Settings sidebar icon visually identical to Today's. Fix: swapped to a proper gear/cog SVG path in `components/layout/Sidebar.tsx`.
#22 [MED] No XP-gained indicator on submit. Fix: `submitScoreAction` returns `{ xpAwarded, isNewPb, best, streakCurrent }`; ResultScreen renders `+N xp` (accent) + `[new PB]` (amber) with opacity-fade animation.
#21 [MED] Sidebar visible during gameplay competed with the focal game stage. Fix: `<AppShell noSidebar>` prop on `/play/[game]`; topbar persists.
#20 [MED] Game stage not centered. Fix: `.app-main-centered` (max-w 720, margin 0 auto, padding 48 32) used when AppShell `noSidebar` is set.
#18 [LOW] No XP bar surfaced; `profiles.xp` unread. Fix: `components/ui/XpBar.tsx` in topbar; `xp` populated live by `process_submission()` -> `xp_events`.
#17 [MED] `/play/[game]` "<- today" back link looked unclickable. Fix: added `.nav-back` class with hover-color + dashed underline.
#13 [HIGH] Dev CSP missing `unsafe-eval` -> React dev bundle crashed onboarding. Fix: gate `unsafe-eval` + `ws://localhost:*` behind `NODE_ENV==="development"`. Production strict.
#12 [HIGH] N-Back leaked timeouts on unmount. Fix: collect ids in `Set<>`, `forEach(clearTimeout)` on cleanup.
#11 [HIGH] Invalid `<a><button>` across today/play/ResultScreen. Fix: `.btn-link` class for server-component nav; ResultScreen uses `router.push`.
#10 [LOW] Signup lacked confirm-password. Fix: `confirm_password` input + client+server validation.
#9 [LOW] Onboarding "let's play -&gt;" rendered literally (JSX entity inside JS string expr doesn't decode). Fix: use literal `->`.
#8 [CRITICAL] Onboarding hit `permission denied for table profiles`. Mgmt-API migration path skipped Supabase default-priv seeding. Fix: 0006 `GRANT ALL` + `ALTER DEFAULT PRIVILEGES` on public schema.
#7 [HIGH] Email-confirm 404: route lived in `app/(auth)/callback/route.ts` but `(auth)` is a route group (no URL contribution). Fix: moved to `app/auth/callback/route.ts`.
#6 [HIGH] Signup "Database error saving new user". `generate_friend_code()` scanned dropped column + missing `extensions` in search_path for pgcrypto. Fix: 0005 rebinds collision check to `profile_secrets` and adds `extensions`.
#5 [HIGH] `friend_code` on profiles publicly enumerable via PostgREST. Fix: 0004 moves to `profile_secrets` (owner-only RLS) + `find_user_by_friend_code(text)` RPC.
#4 [HIGH] Open-redirect via `next` form param. Fix: `safeNext()` validates same-origin.
#3 [HIGH] Proxy didn't redirect signed-in user without profile. Fix: missing profile -> `/onboarding`.
#2 [HIGH] Proxy `/profile/` allowlist matched `/profile/me/*` private namespaces. Fix: `isPublicPath()` excludes `/profile/me*` before allowing `/profile/<username>`.
#1 [HIGH] Proxy redirect dropped Supabase session cookies refreshed during `getUser()`. Fix: copy `response.cookies.getAll()` onto the redirect.
